{
  "name": "krogerbuddy-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the Kroger API for KrogerBuddy.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
